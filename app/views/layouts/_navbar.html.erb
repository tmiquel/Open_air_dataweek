<div class="navbar-floating">
  <div class="container">
    <div class="header">
      <!-- Navbar -->
      <nav class="navbar navbar-expand-lg  navbar-opaque navbar- bg-light ">
        <div class="container navbar-container">
          <!-- Brand/Logo -->
          <a class="navbar-brand" href="../../index.html">
            <%= image_tag asset_path("logo/logo-1-a.png"), class:"", alt:"Open Air" %>
          </a>
          <!-- HAMBURGER MENU (WHEN COLLAPSED <-> SMALL SCREEN) -->
          <div class="d-inline-block">
            <!-- Navbar toggler  -->
            <button class="navbar-toggler hamburger hamburger-js hamburger--spring" type="button" data-toggle="collapse" data-target="#navbar_main" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
            <span class="hamburger-box">
              <span class="hamburger-inner"></span>
            </span>
            </button>
          </div>
          <div class="collapse navbar-collapse align-items-center justify-content-around" id="navbar_main">
            <!-- Navbar links -->
            <!-- LEFT ALIGNED NAVBAR  -->
            <ul class="navbar-nav">
              <% unless (controller_name == "topics" && action_name == "index") %>
              <li class="nav-item dropdown megamenu">
                  <%= link_to "Accueil", root_path, class: "nav-link"  %>
              </li>
              <% end %>
            </ul>
            <!-- RIGHT ALIGNED NAVBAR -->
            <ul class="navbar-nav ml-lg-auto">
              <li class="nav-item">
                <% unless user_signed_in? %>
                <% unless (controller_name == "devise") %>
                <!-- Sign in button shall not appear when user is  in  the sign in page -->
                <%= link_to "S'inscrire", new_user_registration_path, class: "nav-link dropdown-item"  %>
                <% end %>
                <% else %>
                <%= link_to "Mon profil", edit_user_registration_path, class: "dropdown-item"  %>
                <% end %>
              </li>
            </ul>
          </div>
          <!-- BUTTON CALL TO ACTION LEFT, OUTSIDE HAMBURGER MENU -->
          <div class="pl-4 d-none d-lg-inline-block">
            <% unless user_signed_in? %>
            <% unless (controller_name == "devise") %>
            <!-- Sign in button shall not appear when user is  in  the sign in page -->
            <%= link_to "Se connecter", new_user_session_path(anchor: "top"), class: "btn btn-styled btn-base-1 btn-circle btn-teal-blue" %>
            <% end %>
            <% else %>
            <%= button_to "Se déconnecter", destroy_user_session_path, :method => :delete, class: "btn btn-styled btn-base-1 btn-circle btn-teal-blue" %>
            <% end %>
          </div>
        </div>
        
      </nav>
    </div>
  </div>
</div>
